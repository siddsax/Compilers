scope name append
function call args
array manipulation
type checking
classes = struct + function
chars
Add '||' and '&&' and '^'(XOR) and '&' and '==' and '~=' and '<=' and '>=' and '<' and '>'
Fix order of operations in - and /
