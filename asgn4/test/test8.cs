using System;
using compilersareshit;
using lol;
public class goal
{
   public void foo( )
   {

   }

   goal(int a, int[] b){
       a = foo();
       b = foo(foo(c, 'a'));
       while ((left <= left_end) && (mid <= right))
       {
           if (numbers[left] <= numbers[mid])
               temp[tmp_pos++] = numbers[left++];
           else
               temp[tmp_pos++] = numbers[mid++];
       }
   }

   ~goal(){
       int b;
       while ((left <= left_end) && (mid <= right))
       {
           if (numbers[left] <= numbers[mid])
               temp[tmp_pos++] = numbers[left++];
           else
               temp[tmp_pos++] = numbers[mid++];
       }
   }   


}
